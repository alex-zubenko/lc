<?php
namespace Drupal\mtc_core\Manager;

interface A7ManagerInterface
{

    /* ! @constant STATE_PROCESSING */
    const STATE_NEW = 1;

    /* ! @constant STATE_PROCESSED */
    const STATE_PROCESSED = 2;

    /* ! @constant STATE_ERROR */
    const STATE_ERROR = 0;
}
